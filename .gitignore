# .gitignore

# 가상 환경 (Virtual environment)
mywebsite_env/
.venv/
venv/

# 데이터베이스 (Database)
*.sqlite3
db.sqlite3

# 파이썬 바이트코드 (Python bytecode)
*.pyc
__pycache__/

# Django 관련 파일 (Django specific)
*.log
*.pot
*.mo
*.orig
.DS_Store
.idea/  # PyCharm 등 IDE 설정 파일
*/media
*/static

# 보안 관련 (Secrets)
# settings.py 파일에 SECRET_KEY를 직접 하드코딩한 경우:
# 이 부분은 프로덕션 환경에서 .env 파일 등으로 관리해야 합니다.
# 하지만 개발 단계에서 settings.py에 있다면 업로드에 포함될 수 있습니다.
# 실제 SECRET_KEY가 노출되지 않도록 주의하세요!
.env
